# AI 实时控制实验版

已有 EXE 为 0.2.0；2026-09-24 晚间新增改动目前通过源码宿主运行，尚未重新打包。GPT 生成结构化场景，本地程序持续定位、匹配怪物、规划并更新按键。它不是已经训练好的全地图检测模型，也没有证明任意地图都能自动练级。

## 当前能力

- 地图和怪物区域来自 GPT 的 `scene.json`，控制代码不包含猴子地图坐标或每图人工配置界面。
- 当前对话可以生成场景；可选 `plan` 命令通过官方 Responses API 调用 GPT-6。两种来源明确区分，未接入 API 时不宣称有后台 GPT。
- 本地跟踪使用 AI 自动选出的外观样本。未见姿态、遮挡和新的怪物种类仍可能漏检，需要新的 GPT 关键帧；尚未替换成经过跨地图训练的视觉网络。
- 人物以帽子、面部、衣服和连续位置为主要线索，名字用于复核及学习姿态。局部搜索保留原始像素；全屏候选搜索在后台运行，结果必须在新截图确认。候选相近时不随意认定其中一个；画面中完全没有可靠身份线索时松键并继续搜索。
- 镜头位移跟踪木平台和绳子上的特征点，减少背景视差的干扰；地形锚点用于校正漂移，重复木纹不能造成远距离地图跳变。
- 战斗先于导航和运动校准；允许对附近不同平台、上下相差不足 90 像素的目标尝试远程攻击。缺少脚下平台标注时，也能在原地转向和攻击。这个高度范围是测试策略，不能保证所有目标都能命中。怪物、宠物排除仍依赖样本，存在漏检。
- 导航先测移动和原地跳跃，再开放跳跃、向上爬绳；可见平台有足够重叠时允许下落到更宽平台重新测量。水平跳距仍是保守估算，不能当作实测通行成功。
- 短暂遮挡保留路线并松键；落地需要连续确认，爬绳失败、走向起跳点超时或意外落到别层会重新规划。候选与实际通过的连接分开记录。
- 支持斜跳接近绳子，并全程保持上键，接近绳子时松开左右；也保留正下方起跳。仅发出上键不代表抓绳成功，持续对齐且相对地形上升超过跳跃高度后才标记抓住，最终仍要确认目标平台。
- 遇到未标注的平台会请求新地图，同时保留当前视觉和原地战斗能力；不会仅因等待地图更新而停掉可以执行的攻击。
- 所有按键有期限。独立监护线程处理超时、失焦和 F11；旧截图和跨地图迟到结果不能继续发键。
- 同时只允许一个新版 AI 控制器发键。回血/回蓝继续交给宠物，没有加入血量控制。

## 源码运行

依赖使用 `requirements-ai.txt`：现有 numpy、opencv-python、mss，加上 DXcam 0.3.0。主显示器优先使用 DXGI 快速采集，其余位置回退 MSS；当前未完成跨显示器的 DXGI 选择。默认不调用模型、不按键。

```powershell
.venv/Scripts/python.exe game_ai.py snapshot --folder captures/my_session
# GPT 读取该目录 request.json 和图片，将结构化结果写入 scene.json。
.venv/Scripts/python.exe game_ai.py observe --folder captures/my_session --seconds 60
# 只在观察结果可靠、原挂机助手已关闭时，从管理员终端运行：
.venv/Scripts/python.exe game_ai.py play --folder captures/my_session --seconds 60 --navigate --record
.venv/Scripts/python.exe game_ai.py stop --folder captures/my_session
# 关游戏后也可回放，不发任何按键：
.venv/Scripts/python.exe game_ai.py replay --folder captures/my_session
```

截图/观察时需要游戏在前台；开始观察或按键默认有 3 秒切换时间。F11 停止按键；`stop` 创建持久 STOP 标记，源码重新运行应使用新会话或由用户移除该标记。图形界面在明确点击开始时清除此标记。

双击 `dist/MapleAIController-v0.2.0.exe` 打开独立实验界面，或用 `.venv/Scripts/python.exe game_ai_gui.py`。EXE 在打开时请求管理员权限；保持窗口打开，可连续进行多轮测试。先新地图截图，再载入 GPT 场景，先只观察后测试按键。窗口尺寸改变、明显换图或无法定位时，需要重新理解地图。单纯移动窗口位置不再废弃地图。

界面默认勾选导航和回放。每轮重新测运动，避免沿用轻功已经失效时的旧参数；已有录制的文件夹会复制场景到新目录，保留历史证据。新截图仍需 GPT 生成匹配本次 request_id 的场景，不能把午间场景直接套到晚上改变的画面。

开发调试可在管理员终端运行 `.venv/Scripts/python.exe game_ai.py host`，保持测试宿主运行，子进程加载最新源码无需反复提权。宿主只接受 `captures/devhost/job.json` 中的固定 play/observe/climb 命令，单轮最长 600 秒、宿主最多两小时。F11 或 `captures/devhost/STOP` 停止；已有 STOP 不会自动清除。这个宿主不随系统启动。

## 在线模型（可选）

在本机安全配置有效 `OPENAI_API_KEY`，不要写进配置、日志或聊天。默认模型是 `gpt-6-astra`，可用 `--model` 明确指定可用的 GPT-6 模型。

```powershell
.venv/Scripts/python.exe game_ai.py plan --folder captures/my_session
.venv/Scripts/python.exe game_ai.py observe --folder captures/my_session --online --seconds 60
```

`--online` 将游戏客户区关键帧发送到官方 OpenAI API，默认约 30 秒更新一次；有独立请求超时和总运行时限。API 错误会显示明确状态，不静默换模型或把本地等待当成成功调用。网络请求不在即时控制线程中。

## 输出与性能

会话目录保存 `latest.png`（识别和平台叠加）、`map.json`、`motion.json`、`status.json`、`frames.jsonl` 和 `report.json`。结束时状态文件也会更新为结束或失败，避免仍显示运行中。频率统计区分总循环与人物/地图定位有效的循环；空等 GPT 不算有效识别。有效识别频率不等于攻击频率。

`active_input_frames` 和 `attack_input_frames` 单独统计实际提交的动作；`verified_edges` 记录连续落地确认后的路线。攻击按键帧数不证明命中或击杀。`keys_released` 记录正常退出后的松键结果。

导航失败的边只临时冷却，按连续失败次数等待 1–4 秒后重新评估，避免永久排除唯一出口。小于 180 像素的窄平台优先寻找更宽的已观察平台，出口冷却时不反复左右踱步；射程内怪物仍可打断等待。移动速度修正要求连续同向行走，上调至少观察 0.35 秒，下调至少 0.75 秒，避免短距离起步降低跳跃距离估计。

人物短暂丢失后，恢复定位不再重置寻找怪物的等待计时，避免连续遮挡让导航一直停在 search。地图失效仍会重新建立场景。开发时让已启动的子进程继续运行，先准备并验证下一版；不要为了编辑源码提前创建当前会话的 STOP。每个子进程有运行时限，不能把后台运行描述为无限持续。

当前会话可提供 `identity_poses.npy`，包含至多三个经确认的 65×52 BGR uint8 人物样本（例如正面和爬绳背面），加载时禁止 pickle，并验证形状和类型。语义场景更新保留前两个人物样本，再加入新姿态。稳定悬挂在已知绳子附近至少 0.15 秒时，可恢复按上；不会把快速坠落判成挂绳。人物漏检时不再用原地攻击兜底，改由恢复层短移并重新定位；仍须画面新鲜、地图有效且游戏在前台。

局部平台外观检测会在靠近已知边界或脚下未标注时，搜索实际画面中的连续支撑面并扩展平台；遮挡或断口无法确认时不拼接。它使用本地图平台样本，尚不是通用地形分割模型。全画面怪物搜索在后台运行，每次结果只在 1.5 秒内用于选择目标平台，攻击仍采用当前近处检测；`navigation_targets` 与近处 `monsters` 分别记录。已处于预设刷怪平台时，不再仅因另一个预设平台存在就跳层。

导航无动作超过约 0.8 秒时，独立恢复层会短移 0.18 秒，再观察；重复失败轮换方向和小跳。持续移动按键却没有位置进展超过 3 秒，也会尝试恢复。攻击会立即接管。新鲜画面、游戏前台、F11 和输入租约仍有效，因此这不是“任何条件下永不停止”的保证。

下跳先按下 120 毫秒，再加跳跃至 260 毫秒，继续保持下至 400 毫秒；输入层保证方向键先于跳跃发送。小台下跳失败且其边缘下方有已确认的宽平台时，改从边缘走下。全量测试曾被现有 Tkinter 析构线程异常中断；此次导航、识别和输入相关的 76 项测试通过。

追怪新增 6 秒无攻击目标超时：即使巡逻仍在移动，也会中断当前路线，优先去全画面识别到敌人的平台，否则探索访问较少的宽平台。射击只接受与主角脚部高度相差小于 35 像素的当前目标；跨层目标用于导航。连续射击超过 3 秒会短暂调整站位并重新判断，这不等于已经识别到伤害或击杀。取消无目标攻击并添加超时回归后，相关测试为 78 项通过。

跳 A：运行入口默认对同层射程内和可跳达的上方目标使用跳 A。同层远处目标观察到离地约 6 像素即可出手，贴身目标至少约 15 像素；上方目标等待进入目标脚部高度 ±35 像素的射击范围。一个交战安排两次独立攻击按键，间隔至少 0.55 秒，第二次接落地后的下一跳；只有输入层接受请求才推进计数。短暂漏检最多等待 0.3 秒且不发攻击，持续丢失则重新选目标。整个交战最长 3.5 秒、最多三次起跳；按键次数不等于命中或击杀。

抓绳要求绳尾垂直距离不超过实测跳高减 12 像素；斜跳距离同时受水平跳距和绳尾高度限制，超出范围先走近。Home 轻功成功提交后每 120 秒到期，站稳且不处于跳 A、抓绳或跨平台动作时补放。切到其他窗口仍会松键。相关离线测试 87 项通过；实时效果、命中率以及稳定超过 10 Hz 尚待验证。

`--record` 使用后台线程保存最多 160 张原始画面，最高 10 张/秒；队列满时丢弃录制帧，不等待磁盘。每帧绑定其原始 GPT 场景，支持关键帧更新后的回放。预览 PNG 的编码也放到后台。录制约占几百 MB，达到上限即停止录制；游戏控制继续。回放输出 `replay_report.json`，不会启动游戏或输入桥接。

2026-09-24 午后两张固定故障截图回放达到人物定位 160/160；晚间更复杂的 160 帧记录中，某轮改动后仅找回 127 帧，因此不能把午后的固定样本结果外推。晚间已实测下平台、斜跳抓绳与攻击输入，仍出现漏检、路线失败和超过 100 ms 的慢帧；没有证明持续全地图练级。最新详细结果以对应会话的 report.json 和原始记录为准。

`frame_to_decision_ms` 从截屏调用开始量到决策/输入提交，不包含画面事件等待采样、游戏自身渲染、技能前摇或服务器处理。它不能单独证明完整反应时间低于 100 ms。离线合成测试不能替代游戏里的识别准确率和未见地图评测。

## 仍需验证

跨地图视觉召回率、相机滚动及多层背景、平台碰撞一致性、角色跳跃校准、真实跨平台动作、UI/特效遮挡、在线 GPT 更新和未见地图的成功率。用户原有助手仍在运行时，只做观察，不能混合按键测试结果。

2026-09-24 晚间更新：跳跃路线连续失败两次后，本轮地图内禁用该边；地图 epoch 改变后重新评估。怪物匹配先去重，再在候选附近排除宠物/玩家；实时模式减少全屏姓名搜索频率并收窄近身搜索区域。evening_27 前 37.61 秒有效决策 23.69 Hz，最差十秒窗 20.1 Hz，截屏至提交 P99 86.57 ms；这是短时该地图结果，不代表完整事件响应或长期保证。录制中有第二次攻击输入，命中与击杀仍未独立确认。

evening_27 完整 152.09 秒测试的实际统计见 captures/evening_27/verification.json；该文件区分输入接受事件与游戏命中。更新后的源码运行在 evening_28，会话有 600 秒时限，发行版 EXE 尚未重新打包。

攻击朝向修复：目标按当前画面相对角色距离选择，保留近处同一跟踪目标，不再依赖交战开始时的屏幕横坐标。输入层接受的左右方向统一更新 applied_facing，包含恢复移动、导航和抓绳。每次攻击提交前核对当前目标侧别；方向不符先松攻击并转向，成功提交方向至少 60 ms 后才允许攻击。此记录证明方向输入已提交，不代表视觉确认游戏转身。目标跨身、消失或更换时停止沿用旧攻击按压。新增回归后 90 项测试通过。

早A时序参考 dist/unified_config.json 的 random_jump 参数：Alt 40–80 ms，松开后等待 30–50 ms，合计 70–130 ms。AI 早A采用最快端 40+30 ms；松开 Alt 必须被输入层接受后才起算间隔，不再等待截图确认离地。目标仍须是当前可见的同层怪物且朝向正确；错过 180 ms 窗口不在下落时补成迟到攻击。上方目标保留高度判定。status.json 新增 jump_attack_timing 统计实际接受请求的起跳到攻击提交间隔，不能把配置的 70 ms 当作实测。91 项相关测试通过。

早A追加输入层定时松键：既有 5 ms 看门狗在 Alt 下发约 40 ms 后松开，并将实际松键时间和交战标识回传控制器，避免等待下一次视觉循环才松 Alt。旧帧不允许重新按下已经结束的同一跳跃脉冲。失焦、F11 与陈旧帧租约优先于脉冲时长。相关测试 93 项通过。

evening_31 初始约 55 秒实测：早A 16 次，跳跃按下到攻击提交中位数 105.28 ms，范围 82.33–175.73 ms。比上一轮约150ms有改善，但尚不能保证每次落在random_jump配置的70–130ms内。数据为输入提交计时，不是命中延迟。
