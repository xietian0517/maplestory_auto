# 晚间测试版本 0.2.0

游戏可以关着；午后开发与验证不需要它运行。新程序：`F:\mxd\dist\MapleAIController-v0.2.0.exe`。

## 开始时

1. 打开游戏进入 CatApril。Shift 攻击、Alt 跳、方向键移动，回血/回蓝由宠物负责。
2. 保持原挂机助手关闭。打开新版 AI 界面后保持它运行，一次管理员授权可用于多轮测试。
3. 重新截取当前地图，由当前对话生成场景。导航和保存回放默认勾选；测试先运行 60 秒，F11 随时停止。

你晚上只需打开游戏并告诉我可以测试；我会继续负责生成地图和检查结果。

## 这一轮要实际确认

- **导航**：从当前平台出发，至少完成一条下落或跳跃连接，以及一次 Alt+上抓绳、爬升、落地；画面落点与 `verified_edges` 一致。
- **攻击**：到达有怪平台后转向并使用 Shift；结合画面确认命中，不能只看日志里出现 attack，也不能把队友带来的经验算成本程序成果。
- **恢复**：宠物盖住部分名字、怪物击退或一次爬绳失败后，能重新定位或换路线；完全丢失定位时松键。
- **性能**：检查 `minimum_10s_valid_hz` 是否超过 10，所有慢帧、等待地图和定位丢失均保留。另观察画面事件到游戏动作的延迟；离线处理速度不能代替这个测试。
- **结束**：`status.json` 为结束/停止状态，按键已松开。

## 已有证据和边界

午间旧版实际完成过抓绳/上爬，但到平台后的确认曾失败；也发送过攻击指令，尚未证明稳定连续练级。新版在关游戏后修复遮挡、镜头累计误差、路线保留、落地确认、超时重规划，完成了真实截图回归和离线状态衔接测试。不能提前宣称新版已经在游戏中跑通。

API 凭据此前无效，本轮继续由当前对话的 GPT 生成地图；本地快速循环不依赖网络。怪物检测仍使用 GPT 选取的外观样本，特殊姿态、严重特效和未见地图仍需验证。

录制目录 `replay/` 保存有上限的原始画面与对应场景。测试结束后无需保持游戏打开，就能用 `game_ai.py replay --folder <会话目录>` 回放分析。
