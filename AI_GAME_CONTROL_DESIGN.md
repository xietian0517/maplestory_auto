# 冒险岛通用地图 AI 实时控制方案

状态：设计，尚未实施或验证性能。适配当前角色 CatApril，Shift 攻击、Alt 跳跃、方向键移动、Home 轻功；回血回蓝由宠物负责。

产品目标：同一游戏内换地图、换怪物后可自动识别、逐步建图和重新规划，不要求用户为每张地图手工框平台、裁怪物模板或编写路线。通用性是待验证能力，不代表任意未知地图、特殊机关和怪物都能零失误处理。

## 目标与边界

- 本地控制循环目标 30 Hz，每秒完成超过 10 次基于新画面的有效决策；重复读取旧截图不计数。
- 画面事件出现到输入提交，验收目标 P99 < 100 ms；同时报告最大值、超时比例、识别失败比例和有效决策频率。
- 决策处理耗时、循环间隔和事件响应延迟分别测量。30 FPS 不自动等于 33 ms 响应。
- Windows 调度、截屏和游戏客户端均可能卡顿，不能承诺每次响应的绝对硬上限。超过时限的动作作废并松键。
- 输入提交不等于游戏动作生效；攻击前摇、技能冷却、游戏渲染和服务器延迟另行测量。
- GPT-6 不进入 100 ms 的同步依赖链。若要求每一帧都由远端 GPT-6 亲自推理，此设计不能满足该要求。
- 首次地图理解、未知怪物适配和路线生成允许异步耗时，不冒充 100 ms 完成。快速循环仍持续检查新画面，可作出等待、松键等即时决定；等待时间计入练级效率指标，不能把持续等待算成战斗能力达标。

## 两个协同循环

快速循环：本地采集 → 通用角色/怪物检测与跟踪 → 更新局部地形 → 执行当前路线和战斗策略 → 按键状态更新。

慢速循环：关键帧、局部地图、行动结果与战斗统计 → GPT-6 分析 → 有版本和有效期的地图解释、目标和策略配置 → 本地校验后切换。

GPT-6 负责分析地图平台、挑选练级位置、确定攻击距离与退让规则、分析失败片段、根据经验/分钟调整策略。初版由当前对话中的 GPT-6 根据导出的资料生成策略；需要无人值守的在线调整时，再接入独立模型调用适配器并确认可用模型、凭据和调用成本。当前对话不能被当作一个可任意调用的后台 API。

模型更新以事件触发并限频，例如常规优化每 15–30 秒最多一次；首次入图、未知目标、卡住和掉台优先触发，重复事件合并。这个数值是请求节奏，不是模型响应保证。等待期间本地继续执行仍有效的策略；没有适用策略时停止移动和攻击。迟到的旧地图/旧版本结果拒绝执行。

## 通用视觉：按类别与行为理解，而非每图一套模板

- 本地轻量视觉模型输出自己、其他玩家、宠物、怪物、NPC、可站立表面、绳梯、传送门和危险区域；平台输出表面线段或分割区域，不能只靠包围框推断落脚点。
- 角色身份结合名字牌、小地图位置和运动连续性确认。名字牌可作为当前角色的一次性配置，不成为每张地图重新配置的要求。
- 检测怪物优先回答“是不是可攻击对象、在哪里、如何运动”，不依赖每种怪物的精确名称。未知外观仍可能漏检或误检，需显式输出未知和置信度。
- GPT-6 对少量关键帧解释陌生对象和地形，给出类别、区域与证据假设；本地用多帧一致性、位置关系、已有标注集及行动结果复核。模型描述不直接变成已确认地形或可靠训练标签。
- 临时跟踪只在已验证类别和外观上启动；不因一个精灵在移动就认作怪物，避免把玩家、宠物和特效混在一起。未知对象的复核完成前不主动追逐。
- 需要跨地图、跨怪物、动作姿态、遮挡、昼夜/特效等样本建立训练和验证集。GPT-6 可辅助标注和发现失败类型；离线训练的本地模型经过评测后才升级，战斗中不临时重训整个模型。
- 原来的猴子模板可以作为已有地图的补充证据和对照基线，不能继续承担通用方案的主要识别工作。

## 自动建图与路线

1. 换图后建立新的地图会话编号。综合场景变化、角色重定位、小地图布局和低频地图名识别确认换图；旧动作和旧路线立即失效，避免只凭一次画面闪烁认错地图。
2. 通过静态背景特征估计镜头运动，把屏幕坐标变成局部地图坐标。特效、怪物和其他玩家不作为静态锚点；定位置信度低时停止长距离导航。
3. 逐步拼接已看到的地形。小地图提供粗略结构和重定位线索，不能凭它恢复未显示的精细平台。未观察区域保留为未知，不猜出完整地图。
4. 建立路线图：平台可站立区间是节点；步行、跳跃、落下、爬绳是带条件的连接。连接记录置信度、用时、失败次数和可否返回；传送门连接只在确认目的地后保存。
5. 单独校准角色的速度、跳高、水平跳距、上下绳条件、攻击范围和前摇。Home 等状态变化选择对应运动参数；新参数没有验证前收紧可到达范围。
6. 从地形和运动参数推导候选路线，检查起跳点、落点、头顶障碍、横向余量和落下风险。候选连接只有在实际通过后才标为已验证；探索优先选择低风险、可返回的连接。
7. 本地路径规划选择下一落脚点，逐帧控制执行并核对是否到达。偏离、击退或跳跃失败即重新定位和规划，不继续播放原有按键序列。
8. 缓存地图结构及已验证连接，下次进入时重新定位并校验可见部分；分辨率、地图版本或结构变化时废弃不适用部分。

地形理解、全图更新和全局规划在独立工作线程中运行；快速循环只读取带时间戳的地图快照并验证脚下及目标落点的局部状态，避免全图规划拖慢战斗反应。

## GPT-6 的运行时作用

进入新图时，结合若干关键帧和已观察的地形，提出适合当前角色的巡回区域、探索顺序与打法。运行中根据经验/分钟、目标密度、空打、受击和通行失败，改变路线与目标优先级；遇到重复失败时提出原因假设和可验证调整。

输出使用受限结构：地图会话与版本、目标区域、候选平台序列、攻击距离、退让条件、失效条件。实际落脚点、可达性和动作持续时间仍由本地控制器验证并逐帧执行。GPT-6 不能靠一张图可靠推断所有碰撞规则。

完整无人值守版本包含在线模型适配器；凭据、模型可用性和实际调用性能是实施依赖，不视为已经具备。没有在线连接时只能运行缓存策略和本地既有能力，不能声称仍有实时 GPT-6 在规划未知地图。

## 本地性能预算

以下全部为待实测的工程预算，不是已有成绩。

| 环节 | 预算 |
| --- | ---: |
| 事件等待下一次采样 | ≤ 33 ms |
| 截取游戏客户区 | ≤ 8 ms |
| 识别和跟踪 | ≤ 30 ms |
| 策略决策 | ≤ 4 ms |
| 按键状态提交 | ≤ 5 ms |
| 调度余量 | ≤ 10 ms |
| 总预算 | ≤ 90 ms |

分阶段预算达标不能代替端到端延迟测试。采集与处理并行，缓冲区只保留最新一帧，不排队处理旧画面。每帧携带单调时钟时间戳、窗口标识、尺寸、帧号；无法得到真实采集时间时采用保守的采集开始时间，并披露测量误差。

## 识别与即时策略

1. 使用通用视觉和局部地图估计镜头位移，区分角色移动与画面滚动。
2. 结合角色检测、名字牌和跟踪定位角色，以局部搜索降低开销；失败后全图重定位，期间不按旧位置追怪。
3. 本地检测模型与时序跟踪识别不同怪物。模型规模、输入分辨率和检测频率必须通过本机预算及跨地图精度测试；不能用只识别单一猴子的成绩证明通用性。
4. 跟踪各目标的位置、速度和置信度；预测攻击生效时的位置。预测时长包含帧龄和实测攻击前摇，并限制外推距离。
5. 只攻击同一可命中高度、射程内的目标；过近就退让，目标离开射程就松开攻击或转向。用短暂目标保持和距离滞回避免左右抖动，确认消失后立即取消目标。
6. 明确区分搜敌、转向、攻击、退让、回位、补 Buff、暂停状态。跳跃和按住攻击跨帧持续，但每帧都允许撤销；不再使用阻塞式的“先按住两秒，再看截图”。
7. Home 根据已识别的 Buff 状态和校准持续时间触发，避免只凭猜测频繁施放。

策略配置包括地图版本、巡回区域、局部路线、可行走边界、最短/最长攻击距离、目标选择规则、恢复条件和 Buff 规则。GPT-6 只能提供通过校验的配置，运行中的控制器不执行模型生成的任意代码。

## 复用现有工程

- `autofarm/vision.py`：现有 capture 每次新建 mss 对象；改为采集线程持有自己的长驻实例，测量是否能达到预算。若不达标再评估其他 Windows 捕获后端。
- `autofarm/platform_guard.py`：保留作已知场景基线，提取坐标转换和过期保护；通用地图不再要求其人工标定文件。将现有 120 ms 过期判断升级为贯穿采集、决策、提交的统一截止时间。
- `autofarm/rope_archer.py`：复用转向确认、距离和回位逻辑；以单调时钟定期调度，避免处理耗时后再固定 sleep 导致频率下降。
- `autofarm/held_input.py`：扩展为按键集合状态管理与短期续租。保留独立松键线程，失焦、过期、异常和 F11 都撤销按键。
- `autofarm/input_bridge.py`：现有短动作接口用于人工测试。实时模式使用长驻本地控制器直接更新按键状态，或新增经验证的本地状态接口；不为每帧启动 Python CLI，不把旧桥接的短动作执行接口误当成实时接口。
- 新增通用感知、镜头跟踪、地图图结构、运动参数校准、路径规划、技能执行、策略配置、指标采集、录像回放和在线模型适配模块；保留已有模式，避免把尚未达标的新循环直接替换现有逻辑。

## 验收顺序

1. 录制与回放：标注角色、怪物、消失/转向等事件，验证动作选择准确率；离线回放只能测算法，不能证明实时捕获延迟。
2. 实时只观察 60 秒：统计采集、识别、决策和帧龄，不发送按键，找出瓶颈。
3. 受控事件测试：在测试窗口按已知时刻改变目标位置，记录输入接收时刻；报告从显示变化到输入接收的测量方式与误差。另用游戏录制观察动作生效延迟。
4. 各代表地图实战运行至少 10 分钟：每个连续 10 秒窗口的有效决策频率 > 10 Hz，目标 30 Hz；总体事件响应 P99 < 100 ms。报告所有超时和识别失败，不能靠丢弃慢帧美化统计。
5. 按地图和怪物种类划分训练、调试与保留测试集，不能将同一录像的相邻帧拆分来充当泛化测试。保留测试包含未预先配置的地形组合和怪物外观；同时包含其他玩家、宠物和特效等干扰。
6. 测试平地、多层平台、绳梯、滚屏、窄落点以及遮挡。未经训练的特殊机关单独列为不支持项，不能悄悄排除失败后声称支持全部地图。
7. 不人工裁模板、不填平台坐标、不手写路线，完成换图重定位、探索、跨平台到达和怪物战斗。记录初次适配耗时、可覆盖区域比例、路线成功率、误认对象和人工介入次数。
8. 同一地图、角色和补给条件下，对照现有方式比较经验/分钟、空打时长、受击、掉台和卡住次数；从入图开始计时，包含建图和 GPT 等待时间。只有反应、通用性与练级效率一起改善才算成功。

实施里程碑：先建立跨地图数据和只观察的感知基准，再完成局部地图与运动参数校准，接着实现跨平台导航及即时战斗，最后接入 GPT-6 的在线策略调整并进行未见地图验收。固定平台只能是中间测试，不是最终交付范围。

## 依据

OpenAI 官方延迟优化文档建议减少串行请求，并在合适场景采用更快的传统算法。本方案的双循环结构是面向本游戏的工程设计，不是官方提供的 100 ms 性能承诺。

- https://developers.openai.com/api/docs/guides/latency-optimization
- https://developers.openai.com/api/docs/guides/latest-model
